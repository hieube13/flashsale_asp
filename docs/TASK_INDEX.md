# TASK_INDEX — Live task tracker

24-task migration plan (22 backend + 2 frontend). Each row is updated by the Agent at the end of every execution.

## How to use

- `Status`: `pending` → `in-progress` → `done`
- `Branch`: `f_task_XXX_slug`
- `Commit`: short hash, filled after `git commit`
- `Completed`: ISO date

## Live status

| ID | Title | Module | Status | Branch | Commit | Completed | Notes |
|----|-------|--------|--------|--------|--------|-----------|-------|
| TASK-001 | solution_scaffold | infra | done | — | — | 2026-07-13 | FlashSale.slnx + 5 src + 4 test projects, build green |
| TASK-002 | shared_contracts | contracts | done | — | — | 2026-07-13 | ResultMessage, PlaceOrderResponse, ResultCode, DTOs, PlaceOrderMqMessage |
| TASK-003 | docker_compose | infra | done | — | — | 2026-07-13 | MySQL 3316 + Redis 6319 + Kafka 9094 (KRaft) |
| TASK-004 | domain_entities | domain | done | — | — | 2026-07-13 | Ticket, TicketDetail, TickerOrder, OrderQueue, OutboxEvent, IdempotencyKey, PaymentTransaction, Booking + enums |
| TASK-005 | infrastructure_data | infra | done | — | — | 2026-07-13 | FlashSaleDbContext (8 entities), IDbConnectionFactory, MySqlConnectionFactory |
| TASK-006 | infrastructure_redis | infra | done | — | — | 2026-07-13 | IRedisInfrasService + RedisInfrasService (StackExchange.Redis), IStockOrderCacheService stub |
| TASK-007 | infrastructure_kafka | infra | done | — | — | 2026-07-13 | IKafkaOrderProducer + KafkaOrderProducer (Confluent.Kafka), KafkaOrderConsumerWorker stub |
| TASK-008 | application_scaffold | application | done | — | — | 2026-07-13 | 9 service interfaces (Ticket, Order, OrderMQ, Payment, Booking, Employee, Event) |
| TASK-009 | api_foundation | api | done | — | — | 2026-07-13 | Program.cs DI + Serilog + Prometheus /metrics + OutboxPublisherWorker stub |
| TASK-010 | observability | api | done | — | — | 2026-07-13 | prometheus-net, Serilog request logging, /metrics, /health + ArchitectureTests (5 NetArchTest rules) |
| TASK-011 | catalog_ticket_slice | catalog | done | `f_task_011_catalog_ticket_slice` | — | 2026-07-13 | Ticket + TicketDetail CRUD + L1/L2 cache + warmup worker — 8 unit tests pass, smoke parity OK |
| TASK-012 | order_read_slice | catalog | done | `f_task_012_order_read_slice` | — | 2026-07-14 | Dapper dynamic table for `ticket_order_{yyyyMM}` — `ITickerOrderRepository` + `TickerOrderRepositoryImpl` (`Infrastructure/Data/Dynamic/`) + `OrderDeductionDomainService` (`ExtractYearMonth` from order_number trailing ts) + `TicketOrderAppServiceImpl` (findAll/findPage/findByOrderNumber) + `OrderController` 3 routes. 10 new unit tests, all green. Stubs: TicketOrderAppServiceStub removed. |
| TASK-013 | order_cas_slice | order | done | `f_task_013_order_cas_slice` | — | 2026-07-14 | Redis Lua atomic decrement + MySQL FOR UPDATE safety net + Dapper insert into `ticket_order_{yyyyMM}`. `IStockOrderCacheService` moved from `Infrastructure/Cache/` → `Application/Services/` (abstraction belongs to higher layer). `StockOrderCacheService` now wraps the Lua script (`scripts/decrement_stock.lua` embedded) returning -1/0/1 semantics + handles NOSCRIPT re-eval. On-demand warmup reads from `ticket_item` table and primes both `stock_available` and `price_flash` keys. `TicketOrderAppServiceImpl` gains `PlaceOrderCasAsync`, `DecreaseStockLevel1Async`, `DecreaseStockLevel3CasAsync`, `GetStockAvailableAsync`. `OrderController` exposes `POST /order/cas`, `GET /order/{ticketId}/{quantity}/order|cas`, `GET /order/{ticketId}/{quantity}/{userId}/queued` (queued returns 501 — TASK-015). Order number format `OKX-SGN-{userId}-{seq}-{tsMillis}` and random `userId = Random.Shared.Next(1,10)` preserved (Java quirks). 10 new unit tests (6 PlaceOrderCas scenarios + 2 L3 + 1 GetStock + 1 invalid qty). 43/43 unit + 5/5 arch tests pass. |
| TASK-014 | order_cancel_slice | order | done | `f_task_014_order_cancel_slice` | — | 2026-07-14 | Distributed Redis lock (`LOCK:CANCEL_ORDER:{orderNumber}`, wait 1 s / expiry 5 s) + ownership check + idempotent status flip (already CANCELLED → true, no double restore) + DB stock restore via EF Core `IncreaseStockAsync` + best-effort Redis stock restore. `IDistributedLockProvider` / `IDistributedLock` moved from `Infrastructure/DistributedLock/` → `Application/Services/` to keep the abstraction visible to the Application layer (matches TASK-013's `IStockOrderCacheService` move). New `RedLockFactoryBuilder` (`Infrastructure/DistributedLock/`) wraps `RedLockNet.SERedis.RedLockFactory.Create(endpoints)` so the Api project never sees `RedLockEndPoint`. `RedLockDistributedLockProvider` now backed by real `RedLock.net` (was NoOp stub). `OrderController` exposes `PUT /order/{userId}/{orderNumber}/cancel`. HTTP 200 + `success=false` on lock-busy / not-found / ownership-mismatch / status-update-fail (matches Java semantics, `KNOWN_DIFFERENCES.md` §4). 9 new unit tests. 52/52 unit + 5/5 arch tests pass. |
| TASK-015 | order_mq_producer | order-mq | done | `f_task_015_order_mq_producer` | — | 2026-07-14 | `OrderMqAppServiceImpl.PlaceOrderMqAsync` mirrors Java `OrderMQAppServiceImpl.placeOrderMQ` (lines 38-104): Redis Lua pre-deduct (`DecreaseStockCacheByLuaAsync`) → cache-miss warm + retry → unit-price lookup → atomic transactional write via new `IOrderMqTransactionService` (Application abstraction, EF `IDbContextTransaction` impl in `Infrastructure/Persistence/OrderMqTransactionServiceImpl.cs`) → INSERT `order_queue` + INSERT `outbox_event` in 1 tx. On any throw → compensate Redis via `IncreaseStockCacheAsync` + return FAILED with `INTERNAL_ERROR: …`. Token format `MQ-` + first 16 hex chars of `Guid.NewGuid("N")` (matches Java line 63). Random userId 1-9 preserved (Java quirk — KNOWN_DIFFERENCES #7). EF Core repos: `OrderQueueRepositoryImpl` (CRUD + `UpdateStatusAsync` via `ExecuteUpdateAsync`), `OutboxEventRepositoryImpl` (PENDING-batch read + `MarkPublishedAsync/BatchAsync` for TASK-017). `OrderMQController` exposes `POST /order/mq` (request: `PlaceOrderMqRequest{ticketId, quantity}`) and `GET /order/mq/status/{token}` — both return HTTP 200 with `ResultMessage<T>` (success/failure in body, KNOWN_DIFFERENCES.md §4). DDL for `order_queue` + `outbox_event` added to `environment/mysql/init/01-schema.sql`. `OrderMqAppServiceStub` removed from `Stubs.cs`. 12 new unit tests. 64/64 unit + 5/5 arch tests pass. |
| TASK-016 | order_mq_consumer | order-mq | done | `f_task_016_order_mq_consumer` | — | 2026-07-14 | `KafkaOrderConsumerWorker` (real `IConsumer<string,string>` Confluent.Kafka loop, manual commit, 30s per-message CTS, 3-retry exponential backoff `200/400/800 ms`) calls `OrderMqConsumerHandlerImpl.ProcessAsync` in a fresh DI scope per message. Pipeline (mirrors Java `KafkaOrderConsumer.processOrder` lines 38-82): (1) `IIdempotencyKeyRepository.TryInsertAsync` — INSERT IGNORE, true=new / false=duplicate, skip silently. (2) `ITicketDetailRepository.TryDecreaseStockAsync` — atomic UPDATE … WHERE stock_available ≥ ? . OOS → compensate Redis + flip queue to FAILED `"Hết vé"` (status 2). (3) `IOrderDeductionDomainService.InsertOrder(yyyyMM, order)` — Dapper into `ticket_order_{yyyyMM}` (new domain-service method wired to existing `ITickerOrderRepository.InsertAsync`). (4) Flip queue to SUCCESS (status 1) + carry orderNumber. Order-number format `MQ-{userId}-{tsMillis}` matches Java line 64. New `IIdempotencyKeyRepository` Dapper impl (`Infrastructure/Persistence/Repositories/IdempotencyKeyRepositoryImpl.cs`) — raw `INSERT IGNORE` (Pomelo EF doesn't translate it). `OrderDeductionDomainService.InsertOrder` added (sync seam, async-only repo). `IIdempotencyKeyRepository` interface moved/added in `IRepositories.cs`. DDL `idempotency_key` table appended to `environment/mysql/init/01-schema.sql`. `OrderMqConsumerHandlerStub` removed from `Stubs.cs`. 9 new unit tests cover: happy-path, idempotency-duplicate skip, OOS-compensate, Dapper-insert throw, status-update throw, order-number format `MQ-{userId}-{tsMillis}`, total-amount = unitPrice × quantity, null-message throw, empty-token throw. 73/73 unit + 5/5 arch tests pass. |
| TASK-017 | outbox_publisher | order-mq | done | `f_task_017_outbox_publisher` | — | 2026-07-14 | `OutboxPublisherWorker` (moved from `FlashSale.Api.Workers/` → `FlashSale.Infrastructure/Messaging/`, so the publisher sits with the producer + repos it talks to and becomes directly unit-testable without `InternalsVisibleTo`). Row-by-row cycle every 1 s with 250 ms startup jitter (mirrors Java `OutboxPublisherJob.@Scheduled(fixedDelay=1000)` lines 40-44 + `publishRowByRow` lines 55-80). `ExecuteOnceAsync` (public, drives a single cycle) opens a fresh DI scope, calls `IOutboxEventRepository.FindPendingBatchAsync(500)`, then for each PENDING row: `JsonSerializer.Deserialize<PlaceOrderMqMessage>(payload)` → `IKafkaOrderProducer.SendAndAwaitAckAsync(topic, token, message, ct)` → `MarkPublishedAsync(id, UtcNow, ct)`. Per-row try/catch — Kafka failure or ACK timeout leaves the row PENDING for the next cycle (poison-row escape: malformed JSON skipped without marking, logged at error). Producer config (`EnableIdempotence=true`, `Acks=All`) inherited from TASK-007 — broker confirms before row flips. ArchitectureTests reference updated (`OutboxPublisherWorker` → `PaymentAppServiceStub`) since the worker no longer lives in `FlashSale.Api`. 7 new unit tests: empty batch, happy path (publish + mark both rows), kafka failure keeps PENDING, malformed JSON skip + continue, null payload skip, cancellation aborts, MarkPublished failure caught + cycle continues. 80/80 unit + 5/5 arch tests pass. |
| TASK-018 | payment_vnpay | payment | done | `f_task_018_payment_vnpay` | — | 2026-07-14 | `VnPayGatewayService` real HMAC-SHA512 + URL builder (`TreeMap` sort, US-ASCII URL-encode, replace `+`→`%20`, `&` joiner, fix Java "SECRET" hardcode → config `VnPay:HashSecret`); added `VerifySignature` to interface. `IVnPayGatewayService` extended in-place; stub removed. `PaymentAppServiceImpl` orchestrates: idempotency lookup (`FindByOrderNumberAsync` PENDING/INIT for same user → return existing URL), DB persist `payment_transaction` (IN_PROGRESS), IPN callback handler with `RedLock` key `LOCK:PAYMENT_IPN:{txnRef}` (wait 5s / expiry 10s) → JSON `{RspCode, Message}` response. RspCode mapping: 00=Confirm Success, 01=Order not found, 02=Order already confirmed, 04=Invalid amount, 97=Invalid Signature, 99=Unknown. Sync in-request thread (no outbox/Kafka for IPN). `ticket_order.status` NOT touched (matches Java — KNOWN_DIFFERENCES.md §17). `PaymentRepositoryImpl`: EF Core for static `payment_transaction`, Dapper for `ticket_order_{yyyyMM}` shard lookup via `IOrderDeductionDomainService.ExtractYearMonth`. `IPaymentRepository` extended with `FindByOrderNumberAsync` + `GetOrderAmountAsync`. `PaymentController` exposes 3 routes: `POST /payment/create` (returns `ResultMessage<PaymentUrlResponse>`), `GET /payment/callback/return` (renders minimal HTML — not authoritative), `POST /payment/callback/ipn` (`application/x-www-form-urlencoded` body, signature gate runs in controller before service, async JSON response). `appsettings.json` adds `VnPay` block (TmnCode/HashSecret/BaseUrl/ReturnUrl/IpnUrl/Version/CurrCode/Locale/Command/OrderType). `Program.cs`: `PaymentAppServiceStub` swapped for `PaymentAppServiceImpl`; `UseForwardedHeaders` middleware + `ForwardedHeadersOptions` config so `vnp_IpAddr` resolves from `X-Forwarded-For` first, fallback `Connection.RemoteIpAddress`, then `"0.0.0.0"` (KNOWN_DIFFERENCES.md §18, mirrors TASK-014 RedLock pattern). `ArchitectureTests` marker updated (`PaymentAppServiceStub` → `OrderMQController`). DDL `payment_transaction` table added to `environment/mysql/init/01-schema.sql` (DECIMAL(16,3) amount, PaymentStatus TINYINT status, unique `uk_payment_payment_id`, indexes on `OrderNumber` + `(OrderNumber, PaymentStatus)` + `UserId`). Stubs: `PaymentAppServiceStub` removed. 16 new unit tests: `VnPayGatewayService` (6 — hash/URL/sorted params/amount round/secret-missing throws/sign-then-verify roundtrip/hash-tamper rejected/missing-hash rejected), `PaymentAppServiceImpl` (10 — happy SUCCESS flip, amount mismatch → RspCode 04, txnRef unknown → 01, missing txnRef → 01, lock busy → 02, responseCode 24 → FAILED, already-SUCCESS idempotent ACK, already-FAILED idempotent ACK, BuildAndPersistPayment reuses PENDING URL, BuildAndPersistPayment creates new row). 96/96 unit + 5/5 arch tests pass.
| TASK-019 | employee_timesheet | employee | done | `f_task_019_employee_timesheet` | — | 2026-07-14 | `EmployeeCacheServiceImpl` (Application) + `EmployeeBitSetService` (Infrastructure) implementing `IEmployeeBitSetService` (Application abstraction: SetBitAsync/GetBitAsync/BitCountAsync so Application never reaches into StackExchange.Redis directly). Backed by `IDatabase.StringSetBitAsync` / `StringGetBitAsync` / `StringBitCountAsync`. Key format `user:sign:{userId}:{yyyyMM}` (matches Java), bit offset = `dayOfMonth - 1`. **Pins to UTC** (Java used JVM local zone — KNOWN_DIFFERENCES.md §21). `GetFirstSignDayAsync` scans only `0..lengthOfMonth-1` (fix Java's `0..30` scan — KNOWN_DIFFERENCES.md §22). `GetConsecutiveDaysAsync` walks backward, stops at zero bit, no cross-month lookup (matches Java — KNOWN_DIFFERENCES.md §23). `IEmployeeCacheService` extended from 6 to 8 methods (added `GetMonthlySignDetailsAsync` + `GetSummaryAsync`) with typed DTOs `MonthlySignDetailsDto { TotalSignCount, SignDays }` + `EmployeeSummaryDto { Date, HasSignedIn, MonthlyCount, FirstSignDay, ConsecutiveDays }` (matches Java Map wire-shape but typed — KNOWN_DIFFERENCES.md §24). `EmployeeController` mirrors Java's exact route shape `/api/sign-in/{userId}/*` (8 routes; no auth — KNOWN_DIFFERENCES.md §25) and wraps every response in `ResultMessage<T>` (HTTP 200; success/failure in body — matches our convention). Transient Redis errors (`RedisConnectionException`/`RedisTimeoutException`) wrap to `EmployeeCacheException` → controller returns `ResultMessage` 503 `redis_unavailable`. `IEmployeeCacheService` moved from `IBookingAppService.cs` → own file. `EmployeeCacheServiceStub` removed from `Stubs.cs`. `Program.cs`: `AddScoped<IEmployeeCacheService, EmployeeCacheServiceImpl>` + `AddScoped<IEmployeeBitSetService, EmployeeBitSetService>`. **No DB tables** (cache-only, Java has no DDL). 16 new unit tests (key format, day=1 offset 0, day=31 offset 30, December yyyyMM, idempotent same-day, BITCOUNT proxy, first-day -1 when empty, first-day skip unset bits, February scans only 28 bits, consecutive backward, consecutive stops at zero, monthly sign details one-based inclusive, summary composes 4 reads, day-out-of-range throws, month-arg must be day=1, UTC key generation). 112/112 unit + 5/5 arch tests pass. |
| TASK-020 | booking_demo | booking | done | `f_task_020_booking_demo` | — | 2026-07-14 | `BookingAppServiceImpl` mirrors Java `BookingAppServiceImpl.createBooking` (lines 19-31): validate `ticketId>0` & `quantity 1..10`, persist via `IBookingRepository.AddAsync` (EF Core), map to `BookingDto`. Status defaults to `1` (CONFIRMED) matching Java's `BookingDomainServiceImpl` line 36. `BookingRepositoryImpl` (`Infrastructure/Persistence/Repositories/BookingRepositoryImpl.cs`) — EF Core `AddAsync` + `GetByIdAsync`. `BookingController` exposes `POST /api/bookings` returning `ResultMessage<BookingDto>` HTTP 200 (success/failure in body — KNOWN_DIFFERENCES.md §4). Validation errors (`ArgumentException`/`ArgumentNullException`) → `code=400` body. `BookingDto` field set reduced to 5 (id/ticketId/quantity/bookingCode/status) — **parity with Java** (no `createdAt`). `EventAppServiceImpl` (`SayHi(name)`) returns hardcoded literal `"Hi Infrastructure"` for ANY input — mirrors Java `HiInfrasRepositoryImpl.sayHi(who)` line 10 which IGNORES the `who` arg (KNOWN_DIFFERENCES.md §27). `HiController` (`/hello/*`) exposes 3 endpoints matching Java `HiController` exactly: `GET /hello/hi` (`[EnableRateLimiting("backendA")]` → 2 req / 10 s), `GET /hello/hi/v1` (`[EnableRateLimiting("backendB")]` → 5 req / 10 s), `GET /hello/circuit/breaker` (wraps `https://fakestoreapi.com/products/{1..20}` HTTP call in Polly v8 `checkRandom` ResiliencePipeline: FailureRatio=0.5, MinimumThroughput=5, SamplingDuration=10s, BreakDuration=5s — mirrors `application.yml:81-89`). Fallback body string `"Service fakestoreapi Error!"` matches Java `fallbackCircuitBreaker`. `SecureApiController` (`/api/v1/secure/*`) — Java has NO signature filter and NO mode-switching (only 2 raw echo endpoints). .NET: keeps Java's 2 raw endpoints verbatim (`GET /info` static, `POST /data` echo) + adds 4 extra sub-routes (`/unauthorized` 401, `/forbidden` 403, `/slow` 2s sleep, `/throw` exception) for circuit-breaker / exception-handling smoke testing (dev-mode passthrough, no signature middleware — see Q3/Q4). All SecureApi responses are raw `{status, message, receivedPayload?}` — NO `ResultMessage<T>` wrapper. **`Program.cs` wiring**: `AddRateLimiter(backendA: 2/10s QueueLimit=0, backendB: 5/10s QueueLimit=0)`, `AddResiliencePipeline("checkRandom", FailureRatio=0.5, MinimumThroughput=5, SamplingDuration=10s, BreakDuration=5s)`, `app.UseRateLimiter()`. Stub swap: `BookingAppServiceStub` + `EventAppServiceStub` removed from `Stubs.cs`. `AppResponseStatus` enum not introduced — kept raw int for parity. **No idempotency** — Java's `findByBookingCode` in repo interface was dead code. `BookingRepositoryImpl` exposes `AddAsync` + `GetByIdAsync` only. **`/ticket/ping/java`** — DONE in TASK-011 (TicketDetailController:25-29), `Task.Delay(1000)` + `Ok(new { status = "OK" })`, no change here. **DDL**: `booking` table APPENDED to `environment/mysql/init/01-schema.sql` (Id BIGINT auto-inc, TicketId BIGINT, Quantity INT, BookingCode VARCHAR(64) UNIQUE, Status TINYINT 0=PENDING/1=CONFIRMED/2=CANCELLED, CreatedAt DATETIME default CURRENT_TIMESTAMP, indexes on TicketId + (Status,CreatedAt)) — Java DID NOT ship this DDL (`KNOWN_DIFFERENCES.md` §26). **Package additions**: `Microsoft.Extensions.Http.Resilience 8.10.0` (provides `AddResiliencePipeline` extension). 24 new unit tests (BookingAppServiceImpl 9: happy path + bookingCode regex `BK<ms>[A-Z0-9]{4}` + status=1 + ticketId≤0/quantity-out-of-range/quantity-1/10-boundaries + null request + GenerateBookingCode format + uniqueness ≥99% — mirrors Java birthday-paradox quirk; EventAppServiceImpl 4: returns "Hi Infrastructure" for any name including empty; BookingController 4: null body → 400, ArgumentException → 400, generic exception → 500 "internal", success envelope; SecureApiController 5: info shape, data echo payload, unauthorized/forbidden status, throw exception). 136/136 unit + 5/5 arch tests pass. |
| TASK-021 | parity_tests | testing | done | — | — | 2026-07-14 | **Pre-flight fixes**: `TicketDetailDto` added `Version: long?` field (Java has `version: Long?`); `DecreaseStockQueueAsync` implemented (distributed lock + stub `StartOrderByUserAsync` returning false, mirrors Java's `mqPlaceOrderService.startOrderByUser` always returning false). **Contract test infrastructure**: `FlashSale.ContractTests.csproj` added `FluentAssertions 8.8.0` + `Microsoft.AspNetCore.Mvc.Testing 8.0.10`; `GlobalUsings.cs` for `System.Text.Json`; `ContractTestHttpClient` (`Helpers/`) — HTTP client factory pointing at `TESTING_BASE_URL` env var (default `http://localhost:5080`); `JsonAssertions` (`Helpers/`) — deep JSON compare excluding timestamp/createdAt/updatedAt fields, raw string compare, envelope compare. **5 test files** (40 tests, all `[Fact(Skip=...)]` — require running .NET app + Docker infra): `TicketParityTests.cs` (7 tests: /ticket/active, /ticket/{id}, /ticket/{id}/detail/{id}, /ticket/{id}/detail/{id}?version=, /ticket/{id}/detail/{id}/order raw bool, /ticket/ping/java, POST /ticket/create, DELETE /ticket/{id}); `OrderParityTests.cs` (10 tests: POST /order/cas, OOS failure, GET /order/{t}/{q}/order, GET /order/{t}/{q}/cas, GET /order/{t}/{q}/{u}/queued raw bool, GET /order/{u}/list, GET /order/{u}/list/page, PUT /order/{u}/{n}/cancel, POST /order/mq, GET /order/mq/status/{token}); `BookingDemoParityTests.cs` (9 tests: POST /api/bookings (5 fields, status=1, no createdAt), qty=0 validation, /hello/hi, /hello/hi/v1, /hello/circuit/breaker, /api/v1/secure/info, /api/v1/secure/data, /api/v1/secure/unauthorized 401, /api/v1/secure/forbidden 403); `PaymentParityTests.cs` (4 tests: POST /payment/create returns URL, nonexistent order → failure, /payment/callback/return HTML, /payment/callback/ipn raw `{RspCode,Message}` — NOT ResultMessage); `EmployeeParityTests.cs` (8 tests: all 8 `/api/sign-in/{userId}/*` routes — NOTE: .NET wraps in ResultMessage<T>, Java returns raw types — KNOWN_DIFFERENCES.md §32 documents this). **KNOWN_DIFFERENCES.md**: added §30 (`orderNumber` vs `placeOrderTaskId`/`orderId` in PlaceOrderResponse), §31 (`POST /payment/create` uses JSON body vs Java query params), §32 (Employee + Order endpoints .NET wraps in ResultMessage<T>, Java raw types); main table rows 30/31/32 added. **Build**: 136/136 unit + 5/5 arch + 40 contract (skipped) = 181 total, 0 failures. |
| TASK-022 | cutover | ops | done | — | — | 2026-07-14 | **4 docker-compose override files** (`environment/nginx/`): `docker-compose-shadow.yml` (Phase A: `mirror` directive — shadow traffic to .NET, response from Java); `docker-compose-10pct.yml` (Phase B: `split_clients` 10/90); `docker-compose-50pct.yml` (Phase C: `split_clients` 50/50); `docker-compose-100pct.yml` (Phase D: all → .NET). **4 nginx configs** (`nginx-shadow.conf`, `nginx-10pct.conf`, `nginx-50pct.conf`, `nginx-100pct.conf`) with upstream definitions for `dotnet_api` (`:5080`) and `java_api` (`java-app:1122`, overrideable via `JAVA_HOST` env var). `X-Upstream` header added to trace which backend served. **docs/CUTOVER.md**: full runbook with prerequisites checklist, per-phase commands, monitoring queries (Prometheus/Grafana), business metrics table, load test commands, roles/contacts. **docs/ROLLBACK.md**: rollback decision tree, step-by-step <5min rollback, `X-Upstream` header for backend tracing, post-rollback investigation, contact tree. **tests/FlashSale.LoadTests/k6/flash-sale.js**: adapted from Java k6 baseline, supports `BASE_URL`, `ENDPOINT`, `TICKET_ID`, `QUANTITY`, `STOCK`, `VUS`, `TOTAL_USERS` env vars, CAS/MQ endpoints, oversell guard, `handleSummary` box-art output. Phase A requires Java app reachable as `java-app:1122`; Phase D requires only .NET. |
| TASK-023 | frontend_migrate | frontend | done | — | — | 2026-07-14 | Port `xxxx.fe.com` (React 19 + Vite 8) → `flashsale/frontend/`. `vite.config.js` adds proxy for `/ticket`, `/order`, `/api`, `/hello`, `/metrics`, `/payment`, `/health` → `http://localhost:5080`. `src/services/api.js` all baseURLs converted to relative (`/ticket`, `/order`). All 13 components copied: `BookingSuccessPage`, `CartPage`, `Features`, `Footer`, `Header`, `Hero`, `Home`, `ManagerPage`, `SearchBox`, `TicketDetailPage`, `TicketListing`, `TicketsPage`, `WhyChooseUs`. `src/index.css` (968 lines design tokens + all component styles), `src/App.jsx`, `src/main.jsx` copied verbatim. Public assets: `favicon.svg`, `hero-banner.png`, `icons.svg`. Config files created: `eslint.config.js` (ESLint 9 flat config), `nginx.conf` (SPA fallback + proxy for all backend paths → `flashsale.api:5080`), `Dockerfile` (node:20-alpine → nginx:alpine multi-stage, <50MB), `.dockerignore`, `.gitignore`. `package.json` + `package-lock.json` preserved. `frontend/README.md` documents routes, API endpoints, Docker commands. `docker-compose.yml` adds `frontend` service (build `./frontend`, port `5173:80`, depends on `flashsale.api`, internal DNS `flashsale.api:5080`). `docker-compose.yml` adds `expose: 5080` to `flashsale.api` and healthcheck. |
| TASK-024 | frontend_smoke_e2e | frontend | pending | — | — | — | End-to-end smoke: FE ↔ Api ↔ MySQL/Redis/Kafka cho 10 user-facing endpoints |

## Done (history)

> Archived after each task is committed. The 10 scaffold rows above are kept inline
> for visibility during Phase 0. Once Phase 1 starts (TASK-011), done rows are
> relocated here and the live table is pruned back to pending/in-progress only.
> Phase 3 (TASK-023, TASK-024) covers frontend migration and is appended after the
> backend phases complete.

| ID | Title | Completed | Summary |
|----|-------|-----------|---------|
| TASK-001 | solution_scaffold | 2026-07-13 | `FlashSale.slnx` + 5 src + 4 test projects. Dependency graph: Api → Application → Infrastructure → Domain ← Contracts. |
| TASK-002 | shared_contracts | 2026-07-13 | `ResultMessage<T>`, `PlaceOrderResponse`, `ResultCode` enum, request/response DTOs, `PlaceOrderMqMessage`. Note: `ResultCode` lives in `FlashSale.Contracts.Dto` (not Domain) — kept here for shared use. |
| TASK-003 | docker_compose | 2026-07-13 | MySQL 3316, Redis 6319, Kafka 9094 (KRaft), Prometheus, Grafana. App port 5080. |
| TASK-004 | domain_entities | 2026-07-13 | 8 entities + 4 enums (OrderStatus, OrderQueueStatus, OutboxStatus). No external deps. |
| TASK-005 | infrastructure_data | 2026-07-13 | `FlashSaleDbContext` (EF Core), `IDbConnectionFactory` + `MySqlConnectionFactory` (Dapper). |
| TASK-006 | infrastructure_redis | 2026-07-13 | `IRedisInfrasService` + `RedisInfrasService` (StackExchange.Redis), `IStockOrderCacheService` stub (Lua in TASK-013). |
| TASK-007 | infrastructure_kafka | 2026-07-13 | `IKafkaOrderProducer` + `KafkaOrderProducer` (Confluent.Kafka), `KafkaOrderConsumerWorker` BackgroundService stub. |
| TASK-008 | application_scaffold | 2026-07-13 | 9 service interfaces in `FlashSale.Application.Services`. |
| TASK-009 | api_foundation | 2026-07-13 | `Program.cs` DI, Kestrel :5080, Serilog, Prometheus `/metrics`, `OutboxPublisherWorker` stub. |
| TASK-010 | observability | 2026-07-13 | Prometheus-net, Serilog request logging, `/health`, plus 5 NetArchTest dependency-direction tests enforcing the architecture graph. |
| TASK-011 | catalog_ticket_slice | 2026-07-13 | `TicketAppServiceImpl` + `TicketDetailAppServiceImpl` (Java parity) + `ITicketDomainService`/`TicketDomainServiceImpl` + EF Core `TicketRepositoryImpl` + `TicketDetailRepositoryImpl` (FOR UPDATE CAS) + `ITicketCacheService` + `ITicketDetailCacheService` (2-tier L1 Memory + L2 Redis) + `WarmupDataWorker` (BackgroundService, 5s startup delay, 24h cadence) + `TicketController` (7 endpoints) + `TicketDetailController` (3 endpoints incl. `/ticket/ping/java`) + `TicketMapper`. 8 unit tests pass (Moq TicketRepo + CacheService). Program.cs DI swaps `TicketAppServiceStub`/`TicketDetailAppServiceStub` → real impls, registers `IMemoryCache`. `TicketDto` extended with `PriceOriginal/PriceFlash/StockInitial/StockAvailable` (enriched from first TicketDetail — mirrors Java). `environment/mysql/init/01-schema.sql` for ticket + ticket_item tables. 12 smoke endpoints all return 200 + correct JSON. See also `KNOWN_DIFFERENCES.md` entry 2 (status default), 3 (Ping returns `{"status":"OK"}` not ResultMessage). |
| TASK-013 | order_cas_slice | 2026-07-14 | `IStockOrderCacheService` moved from `FlashSale.Infrastructure.Cache` → `FlashSale.Application.Services` (interface lives in higher layer per architecture rules). `StockOrderCacheService` impl swaps naive INCR/DECR for an embedded Lua atomic-decrement script with `-1/0/1` semantics + NOSCRIPT re-eval. `AddStockAvailableToCacheAsync` now reads `ticket_item` rows for the given activity and primes `PRO_TICKET:{id}:stock_available` + `PRO_TICKET:{id}:price_flash`. `GetEffectivePriceAsync` falls back to MySQL on cache miss and re-primes. `TicketOrderAppServiceImpl.PlaceOrderCasAsync` mirrors Java `placeOrderCAS` (lines 161-220): Redis-Lua → DB FOR UPDATE safety net → insert `TickerOrder` with random userId 1-9 + OKX-SGN-{userId}-{seq}-{tsMillis} format. `DecreaseStockLevel3CasAsync` mirrors Java `decreaseStockLevel3CAS` (lines 96-157). `DecreaseStockLevel1Async` = pure MySQL pessimistic (uses `ITicketDetailRepository.GetStockAvailableAsync` + `TryDecreaseStockAsync`). `GetStockAvailableAsync` = DB read. `OrderController` exposes 4 new routes: `POST /order/cas?ticketId=…&quantity=…`, `GET /order/{ticketId}/{quantity}/order`, `GET /order/{ticketId}/{quantity}/cas`, `GET /order/{ticketId}/{quantity}/{userId}/queued` (TASK-015 stub returns 501). All wrap responses in `ResultMessage<T>`. Tests: 10 new (43/43 unit + 5/5 arch green). Quirks preserved: random userId (`Random.Shared.Next(1,10)`), `Order -> Pending` literal note, `OKX-SGN` terminal_id, 200 + `success:false` body on error. See `KNOWN_DIFFERENCES.md` §3 (random userId — preserved), §8 (outbox userId — preserved). |
| TASK-014 | order_cancel_slice | 2026-07-14 | Distributed Redis lock via real `RedLock.net` (`RedLockDistributedLockProvider` + `RedLockFactoryBuilder`); `IDistributedLockProvider`/`IDistributedLock` abstractions moved from `Infrastructure/DistributedLock/` → `Application/Services/` (mirrors TASK-013 move of `IStockOrderCacheService`). `TicketOrderAppServiceImpl.CancelOrderAsync` mirrors Java `cancelOrder` (lines 439-511): acquire lock → resolve year-month shard via `ExtractYearMonth` → fetch order → ownership check → idempotent if already CANCELLED → `UpdateStatusAsync(2)` → DB `IncreaseStockAsync` → best-effort Redis `IncreaseStockCacheAsync`. `OrderController` exposes `PUT /order/{userId}/{orderNumber}/cancel` (mirrors Java `TicketOrderController.cancelOrder`). All 8 branches return HTTP 200 + `success:true|false` (matches Java + `KNOWN_DIFFERENCES.md` §4). Tests: 9 new — happy path, lock-busy, order-not-found, ownership mismatch, already-cancelled, status-update fail, redis-restore fail, empty orderNumber, lock key naming. 52/52 unit + 5/5 arch green. |
| TASK-015 | order_mq_producer | 2026-07-14 | `OrderMqAppServiceImpl.PlaceOrderMqAsync` mirrors Java `OrderMQAppServiceImpl.placeOrderMQ` (lines 38-104): Redis Lua pre-deduct (`DecreaseStockCacheByLuaAsync`) → cache-miss warm + retry → unit-price lookup → atomic transactional write via new `IOrderMqTransactionService` (Application abstraction, EF `IDbContextTransaction` impl in `Infrastructure/Persistence/OrderMqTransactionServiceImpl.cs`) → INSERT `order_queue` + INSERT `outbox_event` in 1 tx. On any throw → compensate Redis via `IncreaseStockCacheAsync` + return FAILED with `INTERNAL_ERROR: …`. Token format `MQ-` + first 16 hex chars of `Guid.NewGuid("N")` (matches Java line 63). Random userId 1-9 preserved (Java quirk — KNOWN_DIFFERENCES #7). EF Core repos: `OrderQueueRepositoryImpl` (CRUD + `UpdateStatusAsync` via `ExecuteUpdateAsync`), `OutboxEventRepositoryImpl` (PENDING-batch read + `MarkPublishedAsync/BatchAsync` for TASK-017). `OrderMQController` exposes `POST /order/mq` (request: `PlaceOrderMqRequest{ticketId, quantity}`) and `GET /order/mq/status/{token}` — both return HTTP 200 with `ResultMessage<T>` (success/failure in body, KNOWN_DIFFERENCES.md §4). DDL for `order_queue` + `outbox_event` added to `environment/mysql/init/01-schema.sql`. `OrderMqAppServiceStub` removed from `Stubs.cs`. 12 new unit tests. 64/64 unit + 5/5 arch tests pass. |
| TASK-016 | order_mq_consumer | 2026-07-14 | `KafkaOrderConsumerWorker` (real `IConsumer<string,string>` Confluent.Kafka loop, manual commit, 30s per-message CTS, 3-retry exponential backoff `200/400/800 ms`) calls `OrderMqConsumerHandlerImpl.ProcessAsync` in a fresh DI scope per message. Pipeline (mirrors Java `KafkaOrderConsumer.processOrder` lines 38-82): (1) `IIdempotencyKeyRepository.TryInsertAsync` — INSERT IGNORE, true=new / false=duplicate, skip silently. (2) `ITicketDetailRepository.TryDecreaseStockAsync` — atomic UPDATE … WHERE stock_available ≥ ? . OOS → compensate Redis + flip queue to FAILED `"Hết vé"` (status 2). (3) `IOrderDeductionDomainService.InsertOrder(yyyyMM, order)` — Dapper into `ticket_order_{yyyyMM}` (new domain-service method wired to existing `ITickerOrderRepository.InsertAsync`). (4) Flip queue to SUCCESS (status 1) + carry orderNumber. Order-number format `MQ-{userId}-{tsMillis}` matches Java line 64. New `IIdempotencyKeyRepository` Dapper impl (`Infrastructure/Persistence/Repositories/IdempotencyKeyRepositoryImpl.cs`) — raw `INSERT IGNORE` (Pomelo EF doesn't translate it). `OrderDeductionDomainService.InsertOrder` added (sync seam, async-only repo). `IIdempotencyKeyRepository` interface added to `IRepositories.cs`. DDL `idempotency_key` table appended to `environment/mysql/init/01-schema.sql`. `OrderMqConsumerHandlerStub` removed from `Stubs.cs`. 9 new unit tests (happy-path, idempotency-duplicate skip, OOS-compensate, Dapper-insert throw, status-update throw, order-number format `MQ-{userId}-{tsMillis}`, total-amount = unitPrice × quantity, null-message throw, empty-token throw). 73/73 unit + 5/5 arch tests pass. |
| TASK-017 | outbox_publisher | 2026-07-14 | `OutboxPublisherWorker` moved from `FlashSale.Api.Workers/` → `FlashSale.Infrastructure/Messaging/` (publisher now sits with the producer + repos it talks to and is directly unit-testable without `InternalsVisibleTo`). Row-by-row cycle every 1 s with 250 ms startup jitter (mirrors Java `OutboxPublisherJob.@Scheduled(fixedDelay=1000)` lines 40-44 + `publishRowByRow` lines 55-80). `ExecuteOnceAsync` (public, drives a single cycle) opens a fresh DI scope, calls `IOutboxEventRepository.FindPendingBatchAsync(500)`, then for each PENDING row: `JsonSerializer.Deserialize<PlaceOrderMqMessage>(payload)` → `IKafkaOrderProducer.SendAndAwaitAckAsync(topic, token, message, ct)` → `MarkPublishedAsync(id, UtcNow, ct)`. Per-row try/catch — Kafka failure or ACK timeout leaves the row PENDING for the next cycle (poison-row escape: malformed JSON skipped without marking, logged at error). Producer config (`EnableIdempotence=true`, `Acks=All`) inherited from TASK-007 — broker confirms before row flips. ArchitectureTests reference updated (`OutboxPublisherWorker` → `PaymentAppServiceStub`) since the worker no longer lives in `FlashSale.Api`. `FlashSale.Infrastructure.csproj` adds `Microsoft.Extensions.Hosting.Abstractions` (BackgroundService). 7 new unit tests (empty batch, happy path publish+mark both rows, kafka failure keeps PENDING, malformed JSON skip+continue, null payload skip, cancellation aborts, MarkPublished failure caught + cycle continues). 80/80 unit + 5/5 arch tests pass. |
| TASK-018 | payment_vnpay | 2026-07-14 | `VnPayGatewayService` real HMAC-SHA512 + URL builder (TreeMap sort + US-ASCII URL-encode + replace `+`→`%20` + `&` joiner). **Java "SECRET" hardcode fixed** → `VnPay:HashSecret` config. `IVnPayGatewayService` extended with `VerifySignature` (constant-time compare). `PaymentAppServiceImpl`: idempotency lookup (existing PENDING row → reuse URL) + DB persist IN_PROGRESS + IPN callback handler with `RedLock` `LOCK:PAYMENT_IPN:{txnRef}` (wait 5s / expiry 10s) → JSON `{RspCode, Message}` (00=Confirm Success, 01=Order not found, 02=Order already confirmed, 04=Invalid amount, 97=Invalid Signature). Sync in-request thread. `ticket_order.status` NOT touched (matches Java). `PaymentRepositoryImpl`: EF Core + Dapper shard lookup via `IOrderDeductionDomainService.ExtractYearMonth`. `PaymentController` exposes `POST /payment/create` (returns `ResultMessage<PaymentUrlResponse>`), `GET /payment/callback/return` (renders HTML — not authoritative), `POST /payment/callback/ipn` (form-encoded, signature gate runs in controller, async JSON response). `UseForwardedHeaders` middleware wired so `vnp_IpAddr` resolves from `X-Forwarded-For` first, then `Connection.RemoteIpAddress`, then `"0.0.0.0"`. `appsettings.json` adds `VnPay` block. Stubs: `PaymentAppServiceStub` removed; ArchitectureTests marker → `OrderMQController`. DDL `payment_transaction` table appended to `01-schema.sql` (DECIMAL(16,3) amount + unique payment_id + indexes). 16 new unit tests (6 VnPayGatewayService + 10 PaymentAppServiceImpl). 96/96 unit + 5/5 arch tests pass. |
| TASK-019 | employee_timesheet | 2026-07-14 | `EmployeeCacheServiceImpl` (Application) + `EmployeeBitSetService` (Infrastructure) wrapping `StackExchange.Redis` native `StringSetBitAsync` / `StringGetBitAsync` / `StringBitCountAsync` through the new `IEmployeeBitSetService` abstraction (so Application stays free of Redis deps). Key format `user:sign:{userId}:{yyyyMM}`, bit offset = `dayOfMonth - 1`, UTC-pinned (Java uses JVM local zone — fix). `GetFirstSignDayAsync` scans only `0..lengthOfMonth-1` (Java scans 0..30 unconditionally — fix). `GetConsecutiveDaysAsync` walks backward, stops at zero bit, no cross-month lookup (matches Java). `IEmployeeCacheService` extended from 6 to 8 methods (`GetMonthlySignDetailsAsync` + `GetSummaryAsync`) with typed DTOs `MonthlySignDetailsDto` and `EmployeeSummaryDto`. `EmployeeController` mirrors Java's exact `/api/sign-in/{userId}/*` route shape (8 routes; no auth — same as Java). All responses wrapped in `ResultMessage<T>` HTTP 200 (matches our convention). Redis transient errors wrap to `EmployeeCacheException` → 503 `redis_unavailable`. `IEmployeeCacheService` moved from `IBookingAppService.cs` → own file; `EmployeeCacheServiceStub` removed. **No DB tables** (cache-only). 16 new unit tests. 112/112 unit + 5/5 arch tests pass. |
| TASK-020 | booking_demo | 2026-07-14 | `BookingAppServiceImpl` mirrors Java `BookingAppServiceImpl.createBooking` (validate `ticketId>0` + `quantity 1..10`, persist via EF Core `IBookingRepository.AddAsync`, map to 5-field `BookingDto` — no `createdAt` parity). `BookingController` `POST /api/bookings` returns `ResultMessage<BookingDto>` HTTP 200; `ArgumentException` → 400, null body → 400, generic exception → 500 "internal". `BookingCode` format `BK<ms><4hex>` matches Java line 44-47. `EventAppServiceImpl.SayHi(name)` returns hardcoded literal `"Hi Infrastructure"` for ANY input (Java `HiInfrasRepositoryImpl.java:10` quirk — KNOWN_DIFFERENCES §27). `HiController` 3 endpoints (`/hello/hi` → `[EnableRateLimiting("backendA")]` 2 req / 10 s; `/hello/hi/v1` → `[EnableRateLimiting("backendB")]` 5 req / 10 s; `/hello/circuit/breaker` → wraps `fakestoreapi.com/products/{1..20}` in Polly v8 `checkRandom` ResiliencePipeline `FailureRatio=0.5, MinimumThroughput=5, SamplingDuration=10s, BreakDuration=5s`). Fallback body `"Service fakestoreapi Error!"` literal match. `SecureApiController` keeps Java's 2 raw echo endpoints (`GET /info` static, `POST /data` echo) + adds 4 sub-routes (`/unauthorized` 401, `/forbidden` 403, `/slow` 2 s sleep, `/throw` exception) for circuit-breaker smoke (dev-mode passthrough — no signature middleware). `Program.cs`: `AddRateLimiter` + `AddResiliencePipeline("checkRandom", …)` + `UseRateLimiter()`. **DDL** `booking` table appended (Java DID NOT ship it — KNOWN_DIFFERENCES §26). 24 new unit tests. 136/136 unit + 5/5 arch tests pass. |
| TASK-021 | parity_tests | 2026-07-14 | Pre-flight fixes: `TicketDetailDto` added `Version: long?` field (parity with Java `version: Long?`); `DecreaseStockQueueAsync` implemented via RedLock + `StartOrderByUserAsync` stub returning false (mirrors Java `mqPlaceOrderService.startOrderByUser` which always returns false). Contract test infrastructure: `FlashSale.ContractTests.csproj` added `FluentAssertions 8.8.0` + `Microsoft.AspNetCore.Mvc.Testing 8.0.10`; `Helpers/ContractTestHttpClient.cs` (HTTP client factory via `TESTING_BASE_URL` env var); `Helpers/JsonAssertions.cs` (deep JSON compare excluding `timestamp`/`createdAt`/`updatedAt`); `GlobalUsings.cs` for `System.Text.Json`. 5 test files (40 tests, all `[Fact(Skip=...)]` — require running .NET app + Docker infra): `Parity/TicketParityTests.cs` (7: `/ticket/*`), `Parity/OrderParityTests.cs` (10: `/order/*` + `/order/mq/*`), `Parity/BookingDemoParityTests.cs` (9: `/api/bookings`, `/hello/*`, `/api/v1/secure/*`), `Parity/PaymentParityTests.cs` (4: `/payment/*`), `Parity/EmployeeParityTests.cs` (8: `/api/sign-in/{userId}/*`). KNOWN_DIFFERENCES.md: §30 `orderNumber` vs `placeOrderTaskId`/`orderId` in `PlaceOrderResponse`, §31 `POST /payment/create` uses JSON body vs Java query params, §32 `.NET` wraps Employee+Order in `ResultMessage<T>` vs Java raw types; main table rows 30/31/32 added. `IOrderDeductionDomainService.StartOrderByUserAsync` added as seam method. 136/136 unit + 5/5 arch + 40 contract (skipped) = 181 tests pass. |
| TASK-022 | cutover | 2026-07-14 | 4 docker-compose override files (`environment/nginx/`): `docker-compose-shadow.yml` (Phase A: `mirror` directive — shadow traffic to .NET, response from Java); `docker-compose-10pct.yml` (Phase B: `split_clients` 10/90); `docker-compose-50pct.yml` (Phase C: `split_clients` 50/50); `docker-compose-100pct.yml` (Phase D: all → .NET). 4 nginx configs (`nginx-shadow.conf`, `nginx-10pct.conf`, `nginx-50pct.conf`, `nginx-100pct.conf`) with upstream definitions for `dotnet_api` (`:5080`) and `java_api` (`java-app:1122`, overrideable via `JAVA_HOST` env var). `X-Upstream` header traces which backend served. `docs/CUTOVER.md`: full runbook with prerequisites checklist, per-phase commands, Prometheus/Grafana monitoring queries, business metrics table, k6 load test commands, roles/contacts. `docs/ROLLBACK.md`: <5min rollback decision tree, step-by-step commands, `X-Upstream` header for backend tracing, post-rollback investigation steps, contact tree. `tests/FlashSale.LoadTests/k6/flash-sale.js`: adapted from Java k6 baseline, supports `BASE_URL`/`ENDPOINT`/`TICKET_ID`/`QUANTITY`/`STOCK`/`VUS`/`TOTAL_USERS` env vars, CAS/MQ endpoints, oversell guard, `handleSummary` box-art output. Phase A requires Java reachable as `java-app:1122`. |

## Definition of Done (per task)

```
[ ] dotnet build FlashSale.slnx                    0 error
[ ] dotnet test tests/FlashSale.UnitTests          green
[ ] dotnet test tests/FlashSale.ArchitectureTests  green
[ ] No hard-coded secrets / connection strings
[ ] Workers: idempotent, explicit Ack/Nack
[ ] Message chain: CorrelationId propagated
[ ] TASK_INDEX.md: status=done, branch, commit
[ ] FLASH_SALE_ARCHITECTURE.md updated (if API/DB changed)
[ ] INTERNAL_ARCHITECTURE.md updated (if entity/worker added)
[ ] KNOWN_DIFFERENCES.md updated (if behaviour diverges)
[ ] Commit: [TASK-XXX] slug: mô tả ngắn
[ ] Suggested commit printed — NEVER auto-commit
```

Per-task checklist (paste into PR description or commit body):

```
[TASK-XXX] YYYY-MM-DD
[x] build        0 error / 0 warning
[x] unit tests   N/N pass
[x] arch tests   green
[x] secrets      none hard-coded
[x] workers      idempotent + ack
[x] correlation  propagated end-to-end
[x] index        TASK_INDEX.md row updated
[x] arch doc     FLASH_SALE_ARCHITECTURE.md §10 endpoints table (if changed)
[x] internal     INTERNAL_ARCHITECTURE.md §11 status (if changed)
[x] diffs        KNOWN_DIFFERENCES.md entry added (if any)
```

## How to update

At the end of each task, Agent must:

1. Mark `[x]` in this row's checklist.
2. Update `Status`, `Branch`, `Commit`, `Completed`.
3. Commit `docs/TASK_INDEX.md` separately with message `docs: update TASK_INDEX [TASK-XXX]`.

## History

- 2026-07-14 — TASK-017 done. OutboxPublisherWorker (row-by-row, 1 s cycle, per-row try/catch) moved to Infrastructure/Messaging; publisher no longer depends on Api. 80/80 unit + 5/5 arch tests pass.
- 2026-07-14 — TASK-018 done. VnPayGatewayService real HMAC-SHA512 + URL builder (Java "SECRET" hardcode → config fix) + VerifySignature; PaymentAppServiceImpl with idempotency lookup + IPN RedLock + JSON {RspCode, Message} response; PaymentController (POST /create, GET /return, POST /ipn). UseForwardedHeaders middleware for vnp_IpAddr. 96/96 unit + 5/5 arch tests pass.
- 2026-07-14 — TASK-019 done. EmployeeCacheServiceImpl + EmployeeBitSetService wrapping StackExchange.Redis BitSet ops via IEmployeeBitSetService abstraction. 8 endpoints under /api/sign-in/{userId}/*. UTC-pinned (Java uses local zone fix). First-sign-day bounded by lengthOfMonth (Java bug fix). Cross-month disabled (matches Java). IEmployeeCacheService extended 6→8 methods + 2 DTOs. 112/112 unit + 5/5 arch tests pass.
- 2026-07-14 — TASK-020 done. Booking slice (POST /api/bookings + EF Core repo + ResultMessage envelope) + HiController rate-limit + circuit-breaker + SecureApiController raw-echo + Polly v8 AddResiliencePipeline. DDL booking table appended (Java missing DDL — KNOWN_DIFFERENCES §26). 136/136 unit + 5/5 arch tests pass.
- 2026-07-14 — TASK-016 done. Real Kafka consumer worker + idempotency_key gate + DB-decrement + insert order + flip queue. 73/73 unit + 5/5 arch tests pass.
- 2026-07-14 — TASK-015 done. OrderMqAppServiceImpl (Redis pre-deduct + atomic tx write via new IOrderMqTransactionService) + OrderMQController. 64/64 unit + 5/5 arch tests pass.
- 2026-07-14 — TASK-014 done. Distributed cancel-order lock + idempotent status flip + DB + Redis stock restore.
- 2026-07-14 — TASK-013 done. Redis Lua atomic decrement + MySQL FOR UPDATE safety net + CAS stock path.
- 2026-07-14 — TASK-012 done. Dynamic monthly shard table `ticket_order_{yyyyMM}` via Dapper + OrderController read slice.
- 2026-07-13 — TASK-011 done. Catalog slice (Ticket + TicketDetail CRUD + L1/L2 cache + warmup worker).
- 2026-07-13 — TASK-001…TASK-010 done. Scaffolding + shared contracts + docker-compose + domain entities + infra + observability + NetArchTest rules.